package com.circularblocks.shapes.shapeSettings;

public record ConnectorCylinderShapeSettings(ShapeSettings meshSettings, int sides, boolean centered, float repeatFrequency, boolean usePlanarMapping) {
}
